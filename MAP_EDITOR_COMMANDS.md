# 맵 에디터 명령어 목록

## 📋 기본 조작

### 🎮 카메라 이동
- **W** - 카메라 위로 이동
- **A** - 카메라 왼쪽으로 이동
- **S** - 카메라 아래로 이동
- **D** - 카메라 오른쪽으로 이동

### 🖱️ 마우스 조작
- **좌클릭 (드래그)** - 타일 배치
- **우클릭 (드래그)** - 타일 제거

---

## 🎨 타일 선택 및 레이어

### 타일 선택
- **Q** - 이전 타일 선택
- **E** - 다음 타일 선택

### 레이어 전환
- **TAB** - 레이어 순환 (Background → Decoration → Terrain → Background)
  - **Background**: 배경 레이어
  - **Decoration**: 장식 레이어
  - **Terrain**: 지형 레이어 (충돌 체크용)

### 깊이 조절
- **↑ (UP)** - 서브레이어 깊이 증가 (0~9)
- **↓ (DOWN)** - 서브레이어 깊이 감소 (0~9)

---

## 🎯 배치 모드

### 모드 전환
- **F** - 자유 배치 모드 ON/OFF
  - **OFF (기본)**: 그리드 기반 배치 (32픽셀 단위)
  - **ON**: 픽셀 단위 자유 배치

### 타일 변형 (자유 배치 모드)
- **[** - 타일 크기 감소 (0.25x 단위, 최소 0.25x)
- **]** - 타일 크기 증가 (0.25x 단위, 최대 4.0x)
- **Shift + ,** (< 키) - 반시계 방향 15도 회전
- **Shift + .** (> 키) - 시계 방향 15도 회전
- **R** - 타일 변형 초기화 (크기 1.0x, 회전 0도)

---

## 💾 파일 관리

### 저장/불러오기
- **Ctrl + S** - 현재 레이어 저장
- **Ctrl + Shift + S** - 현재 레이어 다른 이름으로 저장
- **Ctrl + O** - 현재 레이어 불러오기
- **Ctrl + N** - 현재 레이어 새 맵 생성

### 초기화
- **Ctrl + L** - 현재 레이어 초기화
- **Ctrl + Shift + L** - 모든 레이어 초기화

---

## 🚪 프로그램 제어
- **ESC** - 맵 에디터 종료

---

## 📊 화면 정보 표시

에디터 왼쪽 상단에 다음 정보가 표시됩니다:
- **Layer**: 현재 선택된 레이어
- **Sub-depth**: 서브레이어 깊이 (0~9)
- **Total depth**: 전체 깊이값 (레이어 기본값 + 서브깊이)
- **Mode**: 배치 모드 (GRID/FREE)
- **Scale**: 타일 크기 배율
- **Rotation**: 타일 회전 각도

---

## 💡 사용 팁

### 그리드 모드 (기본)
1. 깔끔한 타일맵 구성에 적합
2. 타일이 32픽셀 그리드에 자동으로 스냅됩니다
3. 대부분의 지형과 구조물 배치에 사용

### 자유 배치 모드 (F 키)
1. 장식 요소나 세밀한 배치에 적합
2. 픽셀 단위로 정확한 위치 지정 가능
3. 크기 조절과 회전으로 다양한 표현 가능
4. 배경 장식, 효과 오브젝트 등에 활용

### 레이어 활용
- **Background (깊이 0~9)**: 가장 뒤, 배경 이미지
- **Decoration (깊이 10~19)**: 중간, 장식 요소
- **Terrain (깊이 20~29)**: 가장 앞, 충돌 체크 지형

### 깊이 시스템
- 각 레이어는 기본 깊이값을 가집니다
- 서브깊이(0~9)를 조절하여 같은 레이어 내에서도 순서 조절 가능
- 낮은 깊이값이 먼저 그려집니다 (뒤에 표시됨)

---

## 📁 저장 파일 구조

레이어별로 분리 저장됩니다:
- `map_bg.json` - 배경 레이어
- `map_decoration.json` - 장식 레이어
- `map_terrain.json` - 지형 레이어

각 레이어를 독립적으로 저장/불러오기 할 수 있습니다.

